#!/bin/bash
# generate the changelog for releases
base="$(readlink -f $(dirname "$0"))"
exec groovy -cp "$base/../src/main/java" -e 'new org.jenkinsci.backend.gitlogparser.App().generateChangeLog(args[0],new File(args[1]))' "$@"